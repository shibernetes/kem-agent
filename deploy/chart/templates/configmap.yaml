apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kem-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kem-agent.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "kem-agent.config" . | nindent 4 }}
